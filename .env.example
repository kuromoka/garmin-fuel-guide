JEV_MODE=mock
HOST=127.0.0.1
PORT=8787
# Generate your own relay token. Use the same token in CIQ settings.
RELAY_TOKEN=
# Only needed for JEV_MODE=jev; never store this key on the watch.
TYPESAFE_API_KEY=
