# Used only when .personal.json sets networkMode to "jev". The personal build
# copies it into the private PRG; never commit, distribute, or share that file.
TYPESAFE_API_KEY=
# These relay development-server settings are never copied into a direct Jev build.
JEV_MODE=mock
HOST=127.0.0.1
PORT=8787
RELAY_TOKEN=
